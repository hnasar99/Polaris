/** Consent feature helpers — Midnight is the eventual source of truth. */
export {
  consentReducer,
  initialConsentState,
} from "@/domain/state/consent";
export {
  DEFAULT_CONSENT_DURATION_DAYS,
  DEFAULT_CONSENT_PURPOSE,
} from "@/domain/consent/types";
