/**
 * Placeholder when Compact output is not present yet.
 * next.config aliases `@polaris/health-contract` here so `next build`
 * succeeds before `npm run compact`.
 */
export const Contract = undefined;
export const ledger = undefined;
