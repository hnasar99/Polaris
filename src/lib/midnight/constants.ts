/** Shared paths / ids that must not pull Compact WASM into the client graph. */
export const POLARIS_ZK_ASSET_PATH = "/zk/polaris-health";
export const POLARIS_PRIVATE_STATE_ID = "PolarisPrivateState" as const;
